/**
 * En JavaScript, el operador de asignación y suma se escribe como +=, no =+. El operador += se utiliza para agregar el valor del lado derecho al valor del lado izquierdo y luego asignar el resultado a la variable.
 */


let x = 5;
x += 3;  // Esto es equivalente a x = x + 3;
console.log(x);  // Salida: 8
