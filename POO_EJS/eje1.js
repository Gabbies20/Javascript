/**
 * Implementa una clase Pila que tenga métodos para agregar elementos (push) y quitar elementos (pop). Asegúrate de que la pila funcione correctamente, es decir, al hacer pop se debe devolver el último elemento que se agregó.
 */

class Pila{
    constructor(){
        this.elementos = [];
    }

    agregar(elemento){
        this.elementos.push(elemento);
    }

    eliminar(elemento){
        
    }
}

